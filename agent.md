# 项目协作规范

目标：准确完成当前任务，以最小且完整的改动解决问题，保持代码可读、可维护、可验证。

## 1. 适用范围与文档维护

- 本文维护开发者与代理的通用工作规则；具体产品需求、项目结构和运行方式以仓库对应文档及实际配置为准，不在本文重复。
- 先遵循运行环境的权限约束和用户当前明确要求，再结合仓库及目标目录的适用规范执行；存在影响任务结果的冲突时说明并澄清，不自行扩大授权。
- 按主题维护带编号的章节，必要时细分小节；同一规则只定义一次。新增独立主题时追加章节，已有主题在原位置修改。
- 只保留明确、可执行的长期规则。用户要求维护规范时，依据实际问题修订；不堆放任务流水、调研记录、空白模板或尚未确定的技术约定。

## 2. 任务理解与范围控制

- 开始前明确本次目标、交付物、已有约束和完成条件。解释、讨论或评审任务按请求交付；实现任务持续推进到修改与必要验证完成。
- 常规细节优先沿用现有约定并自行处理；只有缺失信息会实质改变目标、对外行为或操作授权时，才提出必要问题，同时继续不依赖答案的工作。
- 每处修改都应能说明与当前目标的直接关系。可以同步修改必要的调用方、测试和文档；无关功能、重构、重命名、清理及批量格式化不纳入本次任务。
- “最小改动”以完整解决问题为前提。若必须扩大范围，说明原因和影响，不用掩盖症状的局部补丁替代必要修复。
- 不为假设中的未来需求新增接口、配置、抽象层或依赖。完成条件满足后结束本次任务，不追加无关优化或额外交付物。

## 3. 问题定位与执行效率

- 修改前检查工作区状态，再读取直接相关的说明、实现、调用方和测试。搜索优先使用 `rg`、`rg --files` 或环境中的等效工具，按疑点逐步扩大范围。
- 缺陷修复优先复现问题，利用错误信息、数据流和已有测试形成有证据的判断；无法复现时说明已知事实和未验证假设，不凭猜测连续试改。
- 先查找可复用的代码、组件和项目脚本。仅在本地信息不足或需要核实外部行为时查阅相关官方资料，避免无目的地扫描仓库或扩大调研。
- 简单任务按第 8 节选择直接执行或委派；涉及多步依赖或较大影响的任务先列简短步骤。仅在交付需要时创建计划、报告或辅助文件。
- 独立的读取和检查可并行，存在依赖的操作顺序执行。委派任务时限定目标、文件范围和交付物，避免多人重复调查或同时修改同一文件。
- 操作失败后先判断原因；缺少新证据或条件未变化时，不重复同一失败操作。阻塞时说明具体缺口，并完成其余已授权工作。

## 4. 代码质量

- 遵循现有架构、目录、命名、格式及错误处理约定，优先扩展已有能力；新模式或依赖仅在当前需求确有必要时引入，并说明理由。
- 保持职责清晰、命名准确和控制流易读。避免重复逻辑、过深嵌套和无实际用途的通用封装，不以压缩行数代替可维护性。
- 明确接口输入、输出和边界条件，在适当边界处理无效输入、空值及外部失败；不过度添加没有实际依据的防御分支。
- 错误应可诊断，不静默吞掉异常、不伪造成功、不用未经约定的默认值掩盖失败。
- 修改公共接口、数据格式或配置时，检查受影响调用方及兼容性；行为变化必须与本次需求一致，必要时同步相关文档。
- 复用项目已有依赖及版本约束，不为局部修改无故升级依赖、重写锁文件或更换工具链。
- 注释解释必要的原因、约束和取舍；移除本次修改遗留的无用代码、导入和临时调试内容，不清理无关代码。

## 5. 验证与交付

- 使用仓库已有的运行、测试、构建、格式和类型检查方式；以实际脚本和配置为依据，不编造命令，不为本次小改动搭建无关测试设施。
- 对新增业务逻辑和缺陷修复，按风险补充能验证行为的测试，覆盖相关正常路径、边界或失败场景；不写只重复实现的测试。文档和低影响格式调整采用相应检查即可。
- 先运行与改动直接相关的检查，并完成仓库要求的检查；只有新改动、失败或明确风险需要时才扩大或重复验证。
- 不得仅为使检查通过而删除测试、削弱断言、关闭校验或掩盖错误。区分本次引入的问题与已有失败，不擅自修复无关问题。
- 交付前审查最终差异，确认目标已实现、没有无关改动、必要验证已完成。无法运行的检查说明原因及影响，不将“未验证”描述为“通过”。
- 汇报先说明结果，再简述关键改动、验证情况和实际限制；只提供必要的文件入口，不罗列执行流水或重复已完成的检查。
- 阶段完成后，在对话回复中说明本阶段完成的工作、验证结果和需要用户核查的内容，不另建开发过程或阶段交付记录文档。已有分阶段计划只维护状态，不追加开发流水；必要的功能设计和使用说明按实际变化维护。

## 6. 改动保护与操作授权

- 不得混入、覆盖或回退用户已有的无关改动；发现已有改动时，先辨明本次工作边界。
- 不得提交密码、密钥等敏感信息，也不得将其写入代码、日志或报告。
- 未经用户明确要求，不主动提交或推送代码，不擅自修改已有提交或重写提交历史。
- 删除用户数据、执行不可逆迁移或其他破坏性操作，必须有明确授权；已有授权且属于当前范围的工作不反复索要确认。

## 7. Git 提交规范

生成提交信息前，先查看仓库近期有代表性的提交记录。已有较多提交并形成稳定惯例时，优先沿用其语言、类型、模块命名及标题和正文格式；没有足够历史记录或未形成明确惯例时，使用下述默认格式及类型、标题要求。

无论采用哪种格式，内容表述、提交粒度、检查流程及第 6 节的授权要求均须遵守。

### 7.1 默认格式

```text
【类型】【模块】具体描述

【正文与关联信息】
修改原因、处理方式、行为影响及相关信息
```

### 7.2 要求

- **默认类型**：【新增】新增功能或能力；【修复】修复已有缺陷；【重构】调整结构，不新增功能、不改变行为；【优化】改善性能；【文档】新增或修改文档、说明或注释，不涉及功能变更；【格式】仅调整格式，不改变代码含义；【测试】新增或修改测试用例、测试辅助代码；【构建】修改构建配置、编译脚本或依赖；【集成】修改持续集成、自动检查或发布流水线；【维护】其他维护工作，不得替代已有明确类型；【回退】撤销提交，必须说明对象和原因。
- **默认标题**：模块使用简短、明确的中文名称，优先复用已有命名并保持一致；描述以动词开头，说明改动对象和结果。整行不超过 72 个字符，末尾不加句号，不使用表情符号。
- **表述**：禁止“更新代码”“修复问题”“调整一下”“优化功能”等含糊描述；详细说明放正文。
- **正文**：说明原因、处理方式和行为影响，不得只重复标题或罗列文件名；存在兼容性影响、实现取舍或已知限制时必须说明，关联信息必须真实。
- **粒度**：一次提交解决一个完整且相对独立的问题，相关实现、测试和必要文档可合并；无关功能、重构或批量格式化必须拆分。
- **检查**：生成提交信息前检查实际差异；执行提交前检查暂存区，确保提交内容与信息一致，并遵守第 6 节的改动保护和授权要求。

## 8. 模型选择与 Token 使用

- 先判断任务复杂度、风险、上下文依赖和验证方式，再选择执行模型；目标是在保证正确性与交付完整的前提下减少 Token 和调用开销。
- 对边界明确、低风险且结果容易验证的工作，优先委派给当前可用且能力足够的轻量模型，例如常规脚本、简单测试、文档整理和局部修改；极小任务可直接完成，避免委派成本超过收益。
- 主代理负责范围、接口与验收标准，给子任务提供最少必要上下文、文件范围和交付要求，避免默认传递完整历史或多代理重复处理同一问题。
- 主代理作为监管者检查实际改动和测试证据，并执行关键验证。涉及金额核算、历史时点、并发恢复或跨模块设计时保留主代理审查；任务不确定性高或轻量模型多次无法解决时，及时提高执行模型能力。
- 不以节省 Token 为由省略必要测试、降低质量或跳过用户的分段复查；仅减少重复读取、冗长输出和无收益的多轮委派。用户指定模型或执行方式时优先遵循。
